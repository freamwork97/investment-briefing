# 종가매매 브리핑 — 2026-08-20 (목)

## 시장 개요

| 구분 | 수치 | 등락 | 비고 |
|------|------|------|------|
| KOSPI | 확인 불가 | — | 당일 지수·시세 데이터 접근 차단 |
| KOSDAQ | 확인 불가 | — | 당일 지수·시세 데이터 접근 차단 |
| 개별 종목 | 확인 불가 | — | 거래대금 상위·OHLC·이동평균선 데이터 전무 |

## 5대 조건 스크리닝 결과

**해당 종목 없음 — 검증 가능한 종목 0건**

5대 조건(주가 범위, 거래대금 500억↑, 등락률, 이평선 정배열, 위꼬리 제한)을 검증할 수 있는 **개별 종목 단위 실측 데이터를 단 한 건도 확보하지 못함**. 지어낸 종목명·수치로 표를 채우지 않음.

| 순위 | 종목명(코드) | 현재가(등락률) | 당일 거래대금 | 고가 대비 이격 | 선정 근거 요약 |
|------|------------|-------------|------------|------------|------------|
| — | — | — | — | — | 5대 조건 동시 충족(검증 가능) 종목 없음 |

## 조건별 탈락 분석

### 조건 1~5 전체: 당일(8/20) 개별 종목 데이터 접근 전면 차단

시도한 데이터 소스와 결과:

| 소스 | 방식 | 결과 |
|------|------|------|
| finance.naver.com (거래대금 상위) | curl / WebFetch | 프록시 CONNECT 403 (`connect_rejected`) |
| api.stock.naver.com, m.stock.naver.com | curl | 연결 실패 |
| finance.daum.net | WebFetch | `EGRESS_BLOCKED` |
| markets.hankyung.com | WebFetch | `EGRESS_BLOCKED` |
| kokstock.com (거래대금 상위) | WebFetch | `EGRESS_BLOCKED` |
| infostock.co.kr | WebFetch | `EGRESS_BLOCKED` |
| data.krx.co.kr | curl | 연결 실패 |
| query1.finance.yahoo.com (차트 API) | curl | 연결 실패 |
| openapi.koreainvestment.com | curl | 연결 실패 |
| stockanalysis.com, kr.investing.com | curl | 연결 실패 |
| WebSearch (당일 마감·급등주 검색) | 검색 | 당일자 개별 종목 확정 시세 미확보 |

- 환경변수에 증권사 API 키(KIS AppKey 등) 등 시세 데이터 인증정보가 설정되어 있지 않음을 확인함.
- 조건 1(가격 1천~10만원)·2(거래대금 500억↑)·3(등락률)·4(5>20>60 정배열)·5(고가 대비 -3% 이내) 중 **단 하나도** 실제 데이터로 검증 불가.

## 결론

**오늘도 매매 없음**

세션 환경의 network egress 정책이 국내외 금융 데이터 사이트를 사실상 전부 차단하고 있어 5대 조건 스크리닝이 구조적으로 불가능함.

**⚠️ 이 문제는 8/12 브리핑부터 오늘(8/20)까지 반복 발생 중**이며, 데이터 소스 문제가 해결되지 않는 한 이 루틴은 매일 동일한 "검증 불가" 결과만 반복 산출함. 스크리닝 로직이 아니라 **데이터 파이프라인 자체를 손봐야 하는 상태**임.

### 권장 조치 (택 1)

1. **환경 egress 정책에 금융 도메인 허용 추가** — `finance.naver.com`, `data.krx.co.kr`, `finance.daum.net` 등을 allowlist에 등록
2. **증권사 OpenAPI 연동** — 한국투자증권(KIS) OpenAPI AppKey/AppSecret을 환경변수로 주입하면 시세·일봉·이평선까지 정식 조회 가능 (가장 권장)
3. **전용 시세 MCP 커넥터 연결** — 세션에 국내 증권 데이터 MCP 서버를 붙여 조회

**다음 재진입 조건**: 위 3가지 중 하나가 확보되는 즉시 재스크리닝.

---

## 데이터 검증 노트

- **데이터 수집 방법**: curl 및 WebFetch로 11개 금융 데이터 소스 접속 시도 → 전부 실패(프록시 403 또는 `EGRESS_BLOCKED` 또는 연결 실패). WebSearch로 당일 마감 시황 검색도 개별 종목 확정 데이터 미확보.
- **조건 4(5일 > 20일 > 60일 정배열) 및 조건 5(고가 대비 -3% 이내) 재검증 결과**: 선정 종목이 0건이므로 검증 대상 자체가 존재하지 않음 — **해당 없음(N/A)**. 데이터가 없는 상태에서 정배열·이격도를 추정해 기재하지 않음.
